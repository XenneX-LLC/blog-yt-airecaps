---
const navLinks = [
  { href: '/blog', label: 'Blog' },
];
---

<header>
  <div class="container">
    <a href="/blog" class="logo">
      <div class="logo-icon">▶</div>
      <span class="logo-text">AI <span>Recaps</span></span>
    </a>
    <nav>
      {navLinks.map(link => (
        <a href={link.href}>{link.label}</a>
      ))}
      <a href="https://airecaps.com" class="nav-cta">Get the App</a>
    </nav>
  </div>
</header>
